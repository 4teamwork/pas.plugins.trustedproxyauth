Introduction
============

`pas.plugins.trustedproxyauth` is a PAS plugin which authenticates requests
coming from a trusted proxy where user authentication is delegated to the
proxy. It extracts the login name from a request header that must be set by
the authenticating proxy.

